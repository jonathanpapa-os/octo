<% if current_user.superadmin_role == TRUE %>

    <br><br>
    <h1><%= @account.slug %> page</h1><br>
    <h1>BOOK AN APPOINTMENT NOW! </h1><br>
    <%= render 'form', schedule: @schedule %>

<% else %>

  <% if  current_user.supervisor_role == TRUE %>

    <br><br>
    <h1><%= @account.slug %> page</h1><br>
    <h1>BOOK AN APPOINTMENT NOW! </h1><br>
    <%= render 'form', schedule: @schedule %>

  <% else %>

    <br><br>
    <h1><%= @account.slug %> page</h1><br>
    <h1>BOOK AN APPOINTMENT NOW! </h1><br>      
    <%= render 'form', schedule: @schedule %>

  <% end %>
<% end %>